package lessons

object Difference extends App {
  case class Person(age: Int)

  val people: Seq[Person] = ???

  val people2 = people filter(_.age >= 18)
  val filter   = people filter(_.age >= 18)
  val filtered = people filter(_.age >= 18)
  val valid   = people filter(_.age >= 18)
  val adults  = people filter(_.age >= 18)



}
