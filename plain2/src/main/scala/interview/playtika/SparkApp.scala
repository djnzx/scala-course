package interview.playtika

object SparkApp {

//  SparkConf sparkConf = new SparkConf();
//  SparkSession sparkSession = SparkSession.builder().config(sparkConf).getOrCreate()
//  
//    Dataset<Row> data =
//      sparkSession
//        .read()
//        .parquet(path);
//
//    data.map((MapFunction<Row, Row>) value -> {
//      throw new RuntimeException("gotcha");
//    },
//      RowEncoder.apply(data.schema())
//    );
//
//    List<Row> rows = new ArrayList<>();
//    Dataset<Row> data = sparkSession.read() .parquet(inputPath);
//    data.map((MapFunction<Row, Row>) value -> {
//      rows.add(value);
//      return value
//      RowEncoder.apply(data.schema()))
//
//      .write()
//        .parquet(outputPath);
//
//      print("Rows: ");
//      rows.forEach(row -> print(row));

}
